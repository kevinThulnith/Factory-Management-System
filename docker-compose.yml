services:
  fms-slim-database:
    image: postgres:14-alpine
    container_name: fms-slim-database
    volumes:
      - fms-slim-database-data:/var/lib/postgresql/data
      - fms-slim-logs:/var/log/postgresql
    ports:
      - "127.0.0.1:5432:5432"
    env_file:
      - .env
    networks:
      - fms-slim-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"

  fms-slim-redis:
    image: redis:7-alpine
    container_name: fms-slim-redis
    volumes:
      - fms-slim-redis-data:/data
      - fms-slim-logs:/var/log/redis
    ports:
      - "127.0.0.1:6379:6379"
    networks:
      - fms-slim-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"

  fms-slim-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_CLIENT_ID=${VITE_CLIENT_ID}
    container_name: fms-slim-frontend
    volumes:
      - frontend-dist:/frontend-dist
    env_file:
      - .env
    networks:
      - fms-slim-network
    restart: "no" # one-shot builder — exits after copying files to volume
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  fms-slim-proxy:
    image: nginx:1.25-alpine-slim
    container_name: fms-slim-proxy
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - frontend-dist:/usr/share/nginx/html:ro
      - fms-slim-media-files:/app/media
      - fms-slim-logs:/var/log/nginx
    ports:
      - "80:80"
    networks:
      - fms-slim-network
    depends_on:
      fms-slim-frontend:
        condition: service_completed_successfully
      fms-slim-backend:
        condition: service_started
    restart: unless-stopped
    # Security hardening
    read_only: true
    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"

  fms-slim-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fms-slim-backend
    volumes:
      - fms-slim-media-files:/app/media
      - fms-slim-backend-logs:/app/logs
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    depends_on:
      - fms-slim-database
      - fms-slim-redis
    networks:
      - fms-slim-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"

networks:
  fms-slim-network:
    driver: bridge

volumes:
  fms-slim-database-data:
  fms-slim-media-files:
  fms-slim-redis-data:
  fms-slim-logs:
  fms-slim-backend-logs:
  frontend-dist:
